#include <stdio.h>
#include <stdlib.h>
#include <math.h> //biblioteca para operações matemáticas


// RECEBE UM NÚMERO DO USUARIO IMPRIME SEU QUADRADO E SEU CUBO

/*
int main ()
{
    double num, quadrado, cubo;
    printf("Informe um numero:");
    scanf("%lf", &num);
    quadrado = pow (num, 2);  // é só declarar o número, elevado a 2 de quadrado e 3 de cubo;
    cubo = pow(num, 3);
    printf("O numero ao quadrado eh: %.0f\n", quadrado);
    printf("O numero ao cubo eh: %.0f", cubo);
    return 0;
}
*/
